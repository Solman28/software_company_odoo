<?xml version="1.0" encoding="utf-8"?>
<odoo>
	<data>
        <record id="view_detraction_form" model="ir.ui.view">
            <field name="name">account.detraction.form</field>
            <field name="model">account.detraction</field>
            <field name="mode">primary</field>
            <field name="priority">1</field>
            <field name="arch" type="xml">
                <form string="Registrar Detracción">
                    <header>
                        <button string='Validar' 
                            name="action_validate_detraction" 
                            type="object" class="btn-primary" invisible="state != 'draft'"/>
                        <button string="Cancelar" class="btn-default" name="action_cancel_detraccion"
                            type="object"
                            confirm="Esta seguro de cancelar el registro de detracción?" invisible="state not in ('draft', 'posted')"/>
                        <button string="Cambiar borrador" 
                            type="object"
                            class="btn-default" name="action_draft_detraccion" invisible="state != 'cancel'"/>
                        <field name="state" widget="statusbar" statusbar_visible="draft,posted"/>
                    </header>
                    <header>
                        <field name="pay_state" widget="statusbar" statusbar_visible="to_pay,paid"/>
                        <field name="reconciled" invisible="1"/>
                    </header>
                    <sheet>
                        <field name="move_id" invisible="1"/>
                        <div name="button_box" class="oe_button_box">
                            <button class="oe_stat_button" name="action_open_view_move" type="object" icon="fa-tasks" invisible="not move_id">
                                <div class="o_stat_info">
                                    <span class="o_stat_text">
                                        Asiento contable
                                    </span>
                                </div>
                            </button>
                        </div>
                        <group>
                            <group>
                                <field name="fecha_registro" readonly="1" force_save="1"/>
                                <field name="operation_type" readonly="pay_state == 'paid'"/>
                                <field name="detraccion_id" options="{'no_create': True, 'no_open': True}"/>
                            </group>
                            <group>
                                <field name="nro_constancia" readonly="pay_state == 'paid'" default_focus="1"/>
                                <field name="communication" readonly="state != 'draft'"/>
                                <field name="partner_id"/>
                            </group>
                        </group>
                        <group>
                            <group string="Información del pago">
                                <field name="suitable_national_bank_account_ids" invisible="1"/>
                                <field name="fecha_pago" readonly="pay_state == 'paid'"/>
                                <field name="round_amount_detraction" string="Redondear importe de detracción" readonly="1" force_save="1"/>
                                <field name="amount" string="Monto Detracción" readonly="1" force_save="1" invisible="not round_amount_detraction"/>
                                <field name="amount_real" string="Monto Detracción" readonly="1" force_save="1" invisible="round_amount_detraction"/>
                                <field name="national_bank_account_id" required="1" readonly="pay_state == 'paid'" context="{'default_company_id':id, 'default_is_bank_detraction_account':True}" options="{'no_open': True}"/>
                                <field name="payment_method_id" readonly="pay_state == 'paid'" options="{'no_create': True, 'no_open': True}"/>
                            </group>
                            <group string="Información contable">
                                <field name="currency_id" invisible="True"/>
                                <field name="journal_id" readonly="state != 'draft'" options="{'no_create': True, 'no_open': True}"/>
                                <field name="fecha_movimiento" readonly="state != 'draft'"/>
                                <field name="destination_account_id" readonly="state != 'draft'"/>
                                <field name="origin_move_id" readonly="1" force_save="1"/>
                                <field name="state_origin_move_id" invisible="1"/>
                                <field name="company_currency_id" string="Moneda" readonly="1"/>
                            </group>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- <record id="view_detraccion_tree" model="ir.ui.view">
            <field name="name">account.detraction.tree</field>
            <field name="model">account.detraction</field>
            <field name="arch" type="xml">
                <list string="Detraccion Proveedores" create="0">
                    <field name="journal_id"/>
                    <field name="communication"/>
                    <field name="fecha_pago"/>
                    <field name="state"/>
                    <field name="amount"/>
                    <field name="company_currency_id"/>
                    <field name="pay_state"/>
                </list>
            </field>
        </record> -->

        <record id="view_detraction_tree_customers" model="ir.ui.view">
            <field name="name">account.detraction.tree</field>
            <field name="model">account.detraction</field>
            <field name="arch" type="xml">
                <list string="Detraccion Clientes" create="0">
                    <field name="journal_id"/>
                    <field name="communication"/>
                    <field name="fecha_pago"/>
                    <field name="state"/>
                    <field name="amount"/>
                    <field name="company_currency_id"/>
                    <field name="pay_state"/>
                </list>
            </field>
        </record>

        <!-- <record id="action_detraccion_template_vendors" model="ir.actions.act_window">
            <field name="name">Detracción Proveedores</field>
            <field name="res_model">account.detraction</field>
            <field name="view_mode">list,form</field>
            <field name="domain">[('origin_move_id.move_type','in',['in_invoice','in_receipt'])]
            </field>
            <field eval="False" name="view_id"/>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Haga clic para crear una detraccion.
                </p>
            </field>
        </record> -->

        <record id="action_detraction_template_customers" model="ir.actions.act_window">
            <field name="name">Detracción Clientes</field>
            <field name="res_model">account.detraction</field>
            <field name="view_mode">list,form</field>
            <field name="domain">[('origin_move_id.move_type','in',['out_invoice'])]
            </field>
            <field eval="False" name="view_id"/>
        </record>

        <!-- <record id="action_detraccion_form_vendors" model="ir.actions.server">
            <field name="name">Detracción</field>
            <field name="model_id" ref="model_account_detraction"/>
            <field name="state">code</field>
            <field name="code">
action_values = env.ref('company_electronic_invoicing_base.action_detraccion_template_vendors').read()[0]
action = action_values
            </field>
        </record> -->

        <record id="action_detraction_form_customers" model="ir.actions.server">
            <field name="name">Detracción</field>
            <field name="model_id" ref="model_account_detraction"/>
            <field name="state">code</field>
            <field name="code">
action_values = env.ref('company_electronic_invoicing_base.action_detraction_template_customers').read()[0]
action = action_values
            </field>
        </record>

        <!-- <menuitem action="action_detraccion_form_vendors" id="menu_detraccion_vendors" parent="account.menu_finance_payables" sequence="6"/> -->
        <menuitem action="action_detraction_form_customers" id="menu_detraction_customers" parent="account.menu_finance_receivables" sequence="6"/>

	</data>
</odoo>